#  Weed Detection Edge Dashboard

A Streamlit dashboard for an edge-based weed detection system running on an NVIDIA Jetson
reComputer with IMU-assisted frame filtering.

**Course:** ASM 591: Agricultural Data Visualization & Edge Computing (Spring 2026, Purdue)
**Author:** Sourav Ranjan Mohapatra — `sourav@purdue.edu`

---

## ✨ What this dashboard shows

| Page | Purpose |
|---|---|
|  **Home** | Project overview, KPI strip, objective status |
|  **Model Benchmarking** | Interactive comparison of 21 YOLO variants (v5 → YOLO26) |
|  **Selected Model** | YOLOv8n per-class AP, confusion matrix, sample support |
|  **Jetson Deployment** | PyTorch vs TensorRT FP16 — real measured 49 FPS on edge |
|  **Session Replay** | Scrub through a scouting session: IMU, detections, FPS in sync |
|  **IMU Analysis** | Tests the core hypothesis — does stability filtering improve confidence? |

##  Data sources

- **Real data** — 21-model benchmark (Google Colab A100), YOLOv8n per-class metrics,
  Jetson PyTorch vs TensorRT FPS measurements.
- **Simulated data** (clearly labeled 🧪 in the UI) — three handheld scouting sessions with
  synthetic IMU + detection streams, generated by `data/generate_sim_data.py`. These will be
  replaced with real Jetson-logged sessions in the final project phase.

---

##  Run locally

```bash
git clone https://github.com/Sourav9348/Weed-Dashboard.git
cd Weed-Dashboard

pip install -r requirements.txt
streamlit run streamlit_app.py
```

Dashboard opens at `http://localhost:8501`.

##  Deploy on Streamlit Community Cloud

1. Push this repo to GitHub (public or private with access granted).
2. Go to **[share.streamlit.io](https://share.streamlit.io)** and sign in with GitHub.
3. Click **New app** → pick this repo → set main file to `streamlit_app.py` → **Deploy**.

Deploys in ~90 seconds. Free tier handles this dashboard comfortably.

##  Project structure

```
weed-dashboard/
├── streamlit_app.py          # Home page (entrypoint)
├── utils.py                  # Shared loaders, colors, UI helpers
├── requirements.txt
├── .streamlit/config.toml    # Theme config
├── pages/                    # Streamlit auto-discovers these
│   ├── 1_📊_Model_Benchmarking.py
│   ├── 2_🎯_Selected_Model.py
│   ├── 3_⚡_Jetson_Deployment.py
│   ├── 4_📹_Session_Replay.py
│   └── 5_🧭_IMU_Analysis.py
└── data/
    ├── benchmark_summary.csv          # Real — 21 YOLO models
    ├── parameter_relations.csv        # Real — params/GFLOPs/infer time
    ├── yolov8n_per_class.csv          # Real — per-class AP
    ├── yolov8n_confusion_matrix.csv   # Real — normalized CM
    ├── simulated_sessions.csv         # Simulated — per-frame session logs
    ├── simulated_session_summary.csv  # Simulated — session KPIs
    └── generate_sim_data.py           # Regenerate simulated data
```

## 🔧 Regenerating simulated data

```bash
cd data
python generate_sim_data.py
```

Uses `np.random.seed(42)` so output is reproducible.


## 📬 License & contact

Course project — for academic use. Questions: `sourav@purdue.edu`.
